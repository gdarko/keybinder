# Keybinder

Allows you to remap your keyboard keys on Linux.

# Peresquites

### Libevdev

On Fedora install it as follows:

```bash
sudo dnf install libevdev-devel
```

On Debian/Ubuntu install it as follows:

```bash
sudo apt install libevdev-dev
```


### Other

- Ninja
- Cmake